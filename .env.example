# =============================================================================
# VARIÁVEIS DE AMBIENTE — planejamento-compras-app
# =============================================================================
# Copie este arquivo para .env.local e preencha conforme o seu ambiente.
# O arquivo .env.local nunca deve ser commitado no Git.
#
# Para desenvolvimento local SEM autenticação OAuth,
# deixe VITE_OAUTH_PORTAL_URL e VITE_APP_ID em branco.
# =============================================================================

# URL do portal OAuth externo (ex: https://auth.suaempresa.com)
# Necessário apenas se o deploy usar autenticação OAuth.
# Localmente, pode ficar vazio — o sistema funcionará normalmente.
VITE_OAUTH_PORTAL_URL=

# ID da aplicação registrada no portal OAuth acima.
# Necessário apenas junto com VITE_OAUTH_PORTAL_URL.
VITE_APP_ID=
